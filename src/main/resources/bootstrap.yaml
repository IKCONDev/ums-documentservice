spring:
  application:
    name: UMS-DOCUMENT-SERVICE
  cloud:
    config:
      uri: http://localhost:9004
      label: ums-2.1   # matches the branch in your config server
  config:
    import: optional:configserver:http://localhost:9004
